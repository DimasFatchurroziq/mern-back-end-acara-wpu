{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "outDir": "./dist",
    "rootDir": "./src",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true, // Saya sarankan menambahkan ini juga
    "lib": ["ES2022", "DOM"] // Jika ada kode yang berinteraksi dengan browser/DOM, tambahkan ini. Jika tidak, "lib": ["ES2022"] saja cukup
  },
  "include": [
    "src/**/*.ts", // Ini mencakup semua file .ts di dalam src dan sub-folder-nya
    "src/docs/swagger.ts" // Ini secara eksplisit mencakup swagger.ts
  ],
  "exclude": ["node_modules", "dist"] // Ini penting untuk mengabaikan folder yang tidak perlu dikompilasi
}